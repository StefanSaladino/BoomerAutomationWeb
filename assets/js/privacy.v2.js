document.addEventListener('DOMContentLoaded',()=>{const links=[...document.querySelectorAll('.policy-nav a')],sections=links.map(a=>document.querySelector(a.hash)).filter(Boolean);if(!sections.length)return;const set=id=>links.forEach(a=>{const active=a.hash==='#'+id;a.classList.toggle('is-active',active);active?a.setAttribute('aria-current','location'):a.removeAttribute('aria-current')});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)set(e.target.id)}),{rootMargin:'-20% 0px -70%',threshold:.01});sections.forEach(s=>io.observe(s));set(sections[0].id)});