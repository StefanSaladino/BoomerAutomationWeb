document.addEventListener('DOMContentLoaded',()=>{const links=[...document.querySelectorAll('.chapter-nav a')],sections=links.map(a=>document.querySelector(a.hash)).filter(Boolean);if(!sections.length)return;const set=id=>links.forEach(a=>{const active=a.hash==='#'+id;a.classList.toggle('is-active',active);active?a.setAttribute('aria-current','location'):a.removeAttribute('aria-current')});links.forEach(a=>a.addEventListener('click',()=>set(a.hash.slice(1))));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)set(e.target.id)}),{rootMargin:'-25% 0px -60%',threshold:.05});sections.forEach(s=>io.observe(s));set(sections[0].id)});